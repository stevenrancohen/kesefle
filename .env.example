COINBASE_COMMERCE_API_KEY=
COINBASE_WEBHOOK_SECRET=
SESSION_SECRET=
TEST_USER_ID=

# Optional. Only required for voice-message-to-expense (WhatsApp voice notes).
# The bot works fully without it: text and receipt-photo flows are unaffected.
# Set as an Apps Script Property (PropertiesService), not in this file — this
# example is the Vercel side. Used by _handleVoiceMessage_ to call Whisper.
OPENAI_API_KEY=
