# Kesefle QA Report — 2026-05-26

Method: static read-only audit of HTML/CSS/JS (no browser).
30 findings: 6 HIGH, 12 MED, 12 LOW.

## HIGH (ship-blockers — fix this week)

| # | Issue | Page:line | Fix |
|---|-------|-----------|-----|
| 1 | **No paid-checkout button exists.** `/account?plan=pro` redirects but no PayPal button is wired. Revenue blocker. | account.html:904 + pricing.html:401,425 | Wire `/api/billing/subscribe` to PayPal SDK button on /account when `kesefle_selected_plan` is pro/family |
| 2 | **Admin password hardcoded in client JS** (`const PASSWORD='kesefle2026'`). View source = bypass. | admin.html:766 | Remove client gate. Probe a `requireAdmin`-gated endpoint on load; redirect to /account if 401. |
| 3 | **Reflected XSS in admin user table.** `${u.name}`/`${u.email}`/`${u.phone}` interpolated into innerHTML without escape. Signup name `<img onerror=...>` runs in admin browser. | admin.html:853-867 | Wrap each `${u.x}` with the existing `esc()` helper |
| 4 | **Dark-mode nav dropdowns invisible on hover** — `dark:hover:bg-white dark:hover:text-white`. | index.html:550,561,570,581,591,602 + pricing/demo etc. (×30 occurrences) | Change to `dark:hover:bg-ink-800 dark:hover:text-white` |
| 5 | **"Free plan" CTA hover invisible** — `bg-white ... hover:bg-white hover:text-white`. | pricing.html:375 + index.html:1462 | Replace with `hover:bg-ink-900 hover:text-white` |
| 6 | **Rubik font referenced but not loaded** on 5 pages — H1/H2/H3 fall back to Heebo. | 404, welcome, dashboard, account, en | Add `&family=Rubik:wght@600;700;800;900` to the Google Fonts link |

## MED (12 findings)

7. `en.html` doesn't load Heebo — Hebrew strings in footer fall back.
8. `en.html` links route to Hebrew-only pages — confusing for English users.
9. dashboard "פתח גיליון" button invisible on dark hover.
10. `<input>` fields missing `<label>` — screen readers announce nothing.
11. No skip-to-content link on any page — WCAG 2.4.1 fail.
12. Numbers in Hebrew strings not bidi-isolated — risk of reordering.
13. 404.html title same as homepage — SEO/UX confusion.
14. welcome.html "פתח וואטסאפ" link has `href="#"` until JS sets it.
15. Bot WA number is US (+1 555 640 8123) — trust risk for IL audience.
16. Desktop nav dropdowns missing `aria-expanded`/`aria-haspopup`.
17. pricing comparison table `sticky right-0` may stick to wrong edge in RTL.
18. Service worker may serve stale UI after deploy — bump cache version per build.

## LOW (12 findings)

19. Playground chips missing `role="group"` + `aria-label`.
20. dashboard tx-row `role="button"` div missing Enter/Space keydown handler.
21. admin/launch-monitor uses undefined `headers` variable → silent 401.
22. Inline `width:64px` overrides Tailwind responsive classes.
23. demo WhatsApp mock may clip on 320px viewport.
24. Admin "Site editor coming soon" uses English `alert()`.
25. dashboard error-recovery block hardcoded colors — ignores dark mode.
26. dashboard NPS card fixed positioning may wrap at 320px.
27. `.pill-no`/`.pill-mid` red/yellow on dark bg below WCAG AA contrast.
28. dashboard preview grid `dir="ltr"` with Hebrew labels — consider `dir="auto"`.
29. fonts.gstatic preconnect missing on welcome/account/en.
30. No `prefers-reduced-motion` wrapper around animations.

## Good news (from the agent)
- WhatsApp links use `rel="noopener"`.
- OAuth tokens not persisted to localStorage.
- All images have alt text.
- Mobile pricing tables have scroll-hint.
- Most pages declare `dir="rtl"` correctly.
- No broken `/business`, `/#how`, `/#alternatives` links after PR #28.

## Fix order
HIGH #2 + #3 (security) → HIGH #1 (revenue) → HIGH #4-6 (visible UI bugs) → MED → LOW.
