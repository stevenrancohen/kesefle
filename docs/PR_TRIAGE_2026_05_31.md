# Stale PR triage — 2026-05-31

Triaged the 10 open PRs older than the most recent merge batch (#152–#160).
Used standing approval to close obsolete PRs with explanatory comments; left
ambiguous and still-relevant ones for Steven to review.

PR #161 was created today (2026-05-31) and is out of scope.

## Summary

- **Closed**: 3
- **Keep (Steven to review)**: 6
- **Ambiguous (flagged)**: 1

## Closed PRs

- **#82** (`docs: PROGRESS_DIGEST update — 2026-05-26 evening session`)
  Docs-only snapshot of a May-26 evening session covering PRs #74–#81 — most
  of which are either merged via different branches, superseded, or stale.
  Rewriting that snapshot today adds no value. Conflicts with main.

- **#83** (`fix(bot): NL budget-intent guard — '2000 שח' no longer parses as ₪2`)
  Targets the stale bot version `2026-05-26-objectives-mini`; current is
  `2026-05-29-kolektziot-route-added` after PRs #152–#156 batch. The same
  root cause is addressed more thoroughly by still-open PR #86 (stateful
  `יעד חדש` wizard), which prevents the issue at the routing layer rather
  than patching the objective-handler regex. Conflicting with main.

- **#131** (`bot: backfill 2023-2025 to sikum histori + wire dashboards via B4`)
  Phase-9 migration files (`BACKFILL_HISTORICAL_YEARS.gs` /
  `WIRE_YEAR_HISTORY_LOOKUP.gs`) have been superseded by
  `bot/SHEET_YEAR_SELECTOR_WIRE.gs` on main, further refined by the still-open
  PRs #157 (SUMPRODUCT+`$B$4` pattern) and #160 (REWIRE_DASHBOARD_TO_B4).

## Keep — needs Steven review

- **#81** (`feat(goals-v2): PR-G2-cron — objective reminders Sun/Tue/Thu 20:00 IL`)
  Adds `api/cron/objective-reminders.js` + `vercel.json` cron entry for the
  proactive DM loop (Sun/Tue/Thu 20:00 IL). Dependency PR #79 (data + bot
  commands) is merged but this cron is NOT on main — the loop is incomplete
  without it. Mergeable, clean. Real unmerged feature.

- **#84** (`fix(admin): light-first by default — root-cause fix for the dark unreadable admin`)
  `admin.html` on main still has `applyTheme(localStorage.getItem('kesefle_theme') || 'dark')`
  at line 1094 — the fix has NOT shipped. Some of the PR's CSS changes
  (`#475569`, `#E2E8F0`) were ported in commit `dac2450` on its branch but
  never merged. Conflicts now (later PR-A2 KPI typography landed on top).
  Real fix; needs rebase.

- **#85** (`docs: BOT_MENU_FIRST_POLICY.md — permanent rule from Steven's live bug`)
  `docs/BOT_MENU_FIRST_POLICY.md` does not exist on main. Clean, mergeable
  product-rule doc documenting the menu-first / confirm-before-write policy.
  Pairs naturally with PR #86 which implements it.

- **#86** (`feat(bot): PR-2 menu-first — stateful 'יעד חדש' wizard (cache-backed)`)
  Adds `_handlePendingFlowStep_` + `_objectiveNewStep_` to the bot (3-step
  CacheService-backed wizard for `יעד חדש`). Not on main. Closes the loop on
  the live "2000 → ₪2" bug at the routing layer. Conflicts now (bot
  version drift through #152–#156 batch); needs rebase. Real unmerged
  feature, more thorough fix than #83.

- **#106** (`feat(skills): steven-unblock-checklist — 3-tier 'what do you need from me' format`)
  Adds `.claude/skills/steven-unblock-checklist/SKILL.md`. Not on main (the
  related `honest-scope-report` / `monday-sync-at-turn-end` skills are on
  main but this specific 3-tier format isn't). Mergeable, clean. Personal
  workflow skill — Steven should decide if he uses this format.

- **#107** (`feat(skills): design-from-screenshot — competitor-visual to engineering-spec discipline`)
  Adds `.claude/skills/design-from-screenshot/SKILL.md`. Not on main. The
  4-question decoder for translating competitor screenshots to engineering
  specs. Mergeable, clean. Standalone — no conflict with the existing 99
  skills.

## Ambiguous — needs Steven decision

- **#123** (`feat(skills): 20 project skills for bot reliability, sheet integrity, migration ops`)
  All 20 new skill files do NOT exist on main, BUT the current skill set
  (99 skills) has equivalents for most of them with different names:
  - `bot-classify-trace` -> covered by `bot-trace-message`
  - `bot-deploy-verify` -> covered by `bot-deploy-paste` + `bot-version-bump`
  - `bot-hebrew-keyword-add` -> covered by `bot-add-keyword`
  - `bot-loop-detect` -> covered by `bot-loop-defense`
  - `bot-tenant-isolation-check` -> covered by `bot-test-isolation`
  - `migration-dry-run-review` -> covered by `migration-pattern` +
    `kesefle-migration-dry-run-validator`
  - `sheet-formula-audit` / `sheet-orphan-row-cleanup` -> covered by
    `sheet-broken-formula` + `verify-data-sources-before-formula-repair`
  - `sheet-dashboard-row-validate` / `sheet-historical-orphan-finder` ->
    overlap with `kesefle-sheet-audit` + `kesefle-bot-sheet-dashboard-sync-checker`

  A few may be unique — `kv-orphan-sweep`, `sheet-snapshot-backup`,
  `monday-task-sync`, `sheet-permission-check`, `bot-owner-gate-audit`,
  `bot-sheet-id-swap` — none have direct equivalents in the current skill
  list, though `kesefle-security-privacy-audit` may cover the security
  ones.

  Decision needed: cherry-pick the ~5 unique skills into a small new PR
  and close this one, or merge whole? Recommend cherry-pick — closing the
  bulk PR avoids overwriting refined names + duplicating coverage.
